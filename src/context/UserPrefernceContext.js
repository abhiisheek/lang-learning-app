import { createContext } from "react";

import constants from "../constants/constants";

const UserPrefernceContext = createContext(constants.USER_PREFERNCE_CONTEXT);

export { UserPrefernceContext };
